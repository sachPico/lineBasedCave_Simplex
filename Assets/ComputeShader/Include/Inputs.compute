RWStructuredBuffer<float4> samplePoints;
uint1 numVertexX, numVertexY, numVertexZ;
float1 voxelSize, isoLevel;

int GetIDFromCoord(int3 coord)
{
    int totalVertexLayer = numVertexX * numVertexZ;
    return coord.x + (coord.z*numVertexX) + (coord.y*totalVertexLayer);
}
